name: Build APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Set up Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c

      - name: Install Android SDK Platform
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME "platforms;android-30" "platforms;android-34"

      - name: Install cargo-apk
        run: cargo install cargo-apk

      - name: Build APK
        # Собираем БЕЗ --release, чтобы использовать авто-подпись
        run: cargo apk build

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CubicBattle-APK
          # cargo-apk теперь кладет файл в папку debug
          path: target/debug/apk/cubicbattle.apk
