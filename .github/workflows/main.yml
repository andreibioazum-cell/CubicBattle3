name: Migrate to C
on: 
  workflow_dispatch: # Запуск вручную

jobs:
  burn_rust_build_c:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Delete Rust stuff
        run: |
          rm -rf src/*.rs
          rm -f Cargo.toml
          rm -f Cargo.lock
          mkdir -p jni

      - name: Create CMakeLists.txt
        run: |
          cat <<EOF > CMakeLists.txt
          cmake_minimum_required(VERSION 3.10.2)
          project(cubicbattle)
          add_library(cubicbattle SHARED jni/main.c)
          find_library(log-lib log)
          find_library(android-lib android)
          find_library(egl-lib EGL)
          find_library(gles-lib GLESv3)
          target_link_libraries(cubicbattle android native_app_glue \${log-lib} \${android-lib} \${egl-lib} \${gles-lib})
          EOF

      - name: Create main.c
        run: |
          cat <<EOF > jni/main.c
          #include <EGL/egl.h>
          #include <GLES3/gl3.h>
          #include <android/log.h>
          #include <android_native_app_glue.h>
          #include <stdlib.h>

          #define LOG_TAG "CubicBattle"
          #define LOGI(...) __android_log_print(ANDROID_LOG_INFO, LOG_TAG, __VA_ARGS__)

          void android_main(struct android_app* state) {
              LOGI("C version started! No more blue screens!");
              while (1) {
                  int ident, events;
                  struct android_poll_source* source;
                  while ((ident = ALooper_pollAll(0, NULL, &events, (void**)&source)) >= 0) {
                      if (source != NULL) source->process(state, source);
                      if (state->destroyRequested != 0) return;
                  }
                  // Тут будет рендер
              }
          }
          EOF

      - name: Create NEW CI (build-c.yml)
        run: |
          mkdir -p .github/workflows
          cat <<EOF > .github/workflows/build-c.yml
          name: Build C APK
          on: [push]
          jobs:
            build:
              runs-on: ubuntu-latest
              steps:
                - uses: actions/checkout@v4
                - name: Set up NDK
                  uses: nttld/setup-ndk@v1
                  with:
                    ndk-version: r25c
                - name: Build with CMake
                  run: |
                    cmake -DCMAKE_TOOLCHAIN_FILE=\$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake \
                          -DANDROID_ABI=arm64-v8a \
                          -DANDROID_PLATFORM=android-24 \
                          .
                    make
          EOF

      - name: Commit and Push
        run: |
          git config --global user.name "C-Migrator"
          git config --global user.email "migrator@example.com"
          git add .
          git commit -m "Migration to C: Rust is dead 🦭🔥"
          git push
