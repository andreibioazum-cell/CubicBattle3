[package]
name = "cubicbattle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
android-activity = { version = "0.5", features = ["native-activity"] }
log = "0.4"
android_logger = "0.13"
glam = "0.24"
glow = "0.13" # Для OpenGL
libc = "0.2"  # Для EGL
